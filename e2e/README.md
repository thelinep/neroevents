# E2E

The E2E suite is intentionally grouped by domain. Authentication tests use a deterministic seeded test user; registration should be covered by a separate test once the seed/reset flow is established.
