# Identity E2E acceptance

The E2E suite should create a fresh user through the registration endpoint rather than relying on a pre-seeded password hash. This validates the real M04 registration and hashing path.
